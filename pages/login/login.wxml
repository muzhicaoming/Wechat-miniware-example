<!--pages/login/login.wxml-->
<view class="container">
  <view class="card">
    <view class="title">用户登录</view>
    
    <input 
      class="input" 
      placeholder="请输入用户名" 
      value="{{username}}"
      bindinput="onUsernameInput"
    />
    
    <input 
      class="input" 
      type="password" 
      placeholder="请输入密码" 
      value="{{password}}"
      bindinput="onPasswordInput"
    />
    
    <input 
      class="input" 
      placeholder="请输入昵称（可选）" 
      value="{{nickname}}"
      bindinput="onNicknameInput"
    />
    
    <button class="btn" bindtap="handleLogin">登录</button>
    <button class="btn btn-secondary" bindtap="handleLogout">登出</button>
  </view>
  
  <view class="card">
    <view class="subtitle">说明</view>
    <text class="desc">本应用为纯前端应用，登录信息仅存储在本地，不会与服务器交互。</text>
  </view>
</view>
